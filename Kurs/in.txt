package main

import (
	"fmt"
	"math"
)

func main() {
    var a float64
	var a[5] int
	
	cdt = b * b - 4 * a * e
	
	x1 = (-b + math.Sqrt(cdt)) / (2 * a)
	x2 = (-b - math.Sqrt(cdt)) / (2 * a)
	
	fmt.Println("x1 = ", x1)
	fmt.Println("x2 = ", x2) 
}